<h1>Home Page</h1>
<p><%= link_to 'New Note Page', new_note_path%></p>
<% @notes.each do |note|%>
<p><%= note.title%></p>
<%= link_to 'Show Note', note_path(note) %>
<%= link_to 'Edit Note', edit_note_path(note) %>
<%= link_to 'Destroy Note', note, method: :delete %>
<% end %>
